# Validação final M01–M22

## Integração até o checkpoint funcional — 2026-09-14

Após a aprovação consolidada do frontend, M02–M22 avançaram por S03–S05 até
`READY_FOR_REVIEW/PENDING`. A regressão completa final passou **316/316 em 10,2
min**, serialmente, com browser, Electron 44.3.0, MPV 0.41.0, CPython 3.12.14,
libtorrent 2.1.1.0, SQLite/WAL e o swarm local controlado. Formatação, ESLint,
`tsc --noEmit`, build Turbo/Vite e `release:evidence` passaram. O build mantém o
aviso conhecido de chunk JS acima de 500 kB.

O ensaio de swarm desta rodada registrou stream ready em 2.699 ms, primeiro
frame em 3.053 ms, seek fora do cache em 510 ms e 47/174 pieces concluídas no
primeiro frame. A fila vazia de downloads também foi corrigida para não iniciar
o torrentd durante playback estritamente local.

Essa evidência não aprova o checkpoint humano, não fecha S06–S08 e não substitui
os gates externos: credenciais/provider/Registry, packaging do runtime,
Windows/TV/Sunshine/Moonlight/controle físico, installer, code signing, CI e
promoção. Nenhum milestone M02–M22 está `DONE`.

## Registro histórico da fase frontend

A seção abaixo preserva a evidência da fase mock-first anterior à integração;
afirmações sobre ausência de backend referem-se somente àquele recorte e foram
superadas pela integração S03–S05 documentada acima.

Revisão humana frontend aprovada de forma consolidada; aceites anteriores de
M01–M03 foram preservados. Evidência técnica não é fechamento S08.

## Ambiente e escopo

Checkout real `/Users/douglasprado/www/ushark`; macOS 26.6.2 (25G83), Node 26.8.1, pnpm 11.17.0, Electron 44.3.0 e Playwright 1.63.0. HEAD observado na auditoria: `01fc3d6`; implementação permanece no working tree, incluindo trabalho preexistente preservado. Nenhum commit/merge/deploy/release criado nesta execução.

Renderer React e adapters em `packages/mocks`; shell Electron mínimo com sandbox/contextIsolation, Node desabilitado, navegação e novas janelas bloqueadas. Busca estática no renderer/mocks não encontrou fetch/XMLHttpRequest/WebSocket/indexedDB/imports Node nem endpoints de backend. O único estado durável desta fase é o marcador versionado de onboarding concluído em `localStorage`; configuração, catálogos e demais fixtures permanecem em memória. Nenhum daemon, provider, Registry, MPV, SQLite ou persistência definitiva adicionados.

## Resultados automatizados

- Formatação direta dos arquivos alterados: passou.
- ESLint completo, `tsc --noEmit` e formatação dos arquivos do ajuste atual: passaram.
- Build orquestrado via Turbo/Vite: passou; aviso de chunk acima de 500 kB preservado.
- `pnpm exec playwright test --trace=off`: **141 passaram, zero falhas e zero skips, em 2,2 min**, após build. Inclui Electron, browser, layouts, mocks, renderer organizado por responsabilidade, CSS colocalizado, dois tipos de botão, toolbar de filtros única, card compartilhado nas orientações vertical/horizontal, Recomendados no mesmo trilho da Home, scroll vertical azul, retorno ao topo ao alcançar o primeiro botão, página canônica de Filme, modal de Série, seeds IMDb, Torrent Health, navegação espacial por controle, imagem/GIF dos episódios e chrome do player.
- `git diff --check`: passou.
- Auditoria documental: 22 conjuntos de artefatos presentes; 445 requisitos distintos (FR/NFR/RX) e 80 jornadas UJ inventariados; nenhum novo aceite humano.
- Referências dos três documentos de revisão/auditoria verificadas contra arquivos locais.

Hashes do build local em [build-artifacts.sha256](build-artifacts.sha256). São rastreabilidade deste checkout, não checksum de installer, assinatura de release ou promoção de canal.

Primeira regressão completa: 106 passaram, 1 falhou (2,3 min). A falha foi o locator genérico `getByRole('status')` do teste antigo de atualização de filmes: após M08 havia dois status legítimos, medição e salvamento. O teste foi restringido ao aviso de identificação atualizado, preservando suas verificações de identidade/favorito/sources. Log: [regression-initial.log](regression-initial.log).

Na revalidação após tornar o onboarding não recorrente, uma primeira rodada perdeu o servidor Vite depois de 108 casos verdes; os sete erros foram `ERR_CONNECTION_REFUSED`, antes de qualquer interação. Os sete casos afetados e um caso vizinho passaram 8/8 contra um servidor novo. A rodada completa seguinte, com servidor novo e exclusivo, passou 115/115. O novo teste Electron fecha o primeiro processo e reabre outro com o mesmo perfil, comprovando a entrada direta na Home.

Na revisão visual seguinte, a Home M04 recebeu navegação sobreposta, Hero amplo e trilhos horizontais panorâmicos. O teste novo comprova navegação principal e deslocamento do trilho pelo foco; os testes de layout verificam Hero proporcional, overflow interno e ausência de overflow horizontal da página em 1080p/1440p/4K. A regressão completa daquela rodada passou 116/116.

Depois do seed IMDb, `DiscoveryItem` recebeu `backdrop` opcional: a Home usa as imagens horizontais locais no Hero e nos trilhos, com fallback para pôster, enquanto a busca mantém a capa vertical. Typecheck, lint, build Vite e os 12 testes focados de M04 passaram; a captura `imdb-home-1920.png` foi inspecionada. Naquele ponto, a regressão completa passou 123/123; CI, Windows/TV e controle físico continuaram pendentes.

Na correção do badge, o selo de resolução foi substituído pelo sinal correto de Torrent Health: 1–5 barras crescentes como rede celular e label textual. Resolução permanece como metadata separada. `SelectionPreview` expõe o snapshot substituível; `MockSelectionPreview` fornece valores determinísticos sem alegar probe real. Filmes, Séries, Home, busca, Hero, detalhes e comparação de fontes usam o componente compartilhado.

Na extensão mais recente, cada episódio de Série recebeu o badge. Capa/card/detalhe da série e trilho Séries da Home mostram `Média` calculada sobre `sourceId`s únicos; a mesma source compartilhada por vários episódios conta uma vez. O episódio agrega somente suas próprias sources. As capturas `imdb-series-1920.png`, `imdb-series-detail-1920.png`, `imdb-series-episodes-1920.png` e `imdb-home-1920.png` foram regeneradas e inspecionadas. Dezesseis testes focados M03/M04 e a regressão completa **125/125 em 2,1 min** passaram após build; os três layouts de Séries e Electron offline também permaneceram verdes.

No refinamento posterior de navegação, `navigation.ts` passou a oferecer seleção espacial por lane, vizinhos `data-nav-*`, grupos horizontais, scroll suave/reduced motion, repetição temporizada de D-pad/analógico e mapeamento de setas/OK/Voltar de controles remotos. Confirmar/voltar são edge-triggered para impedir ações duplicadas. Os dois casos novos verificam movimento horizontal, troca vertical de seção mantendo coluna, repetição, modal e restauração de foco; [captura 1920×1080](../../milestones/M01-onboarding/evidence/fluid-navigation-home-1920.png) foi inspecionada. A regressão final passou **127/127 em 2,1 min**; Electron macOS e layouts continuaram verdes.

No ajuste seguinte de M03, cada episódio recebeu espaço fixo para thumbnail 16:9, usando o backdrop da série como prévia até existir arte própria. O detalhe aceita JPEG, PNG, WebP ou GIF de até 12 MB e atualiza detalhe/card na sessão. O teste novo cobre rejeição de texto, envio de GIF e retorno ao card com a arte preservada; [captura dos episódios](../../milestones/M03-series/evidence/imdb-series-episodes-1920.png) foi regenerada e inspecionada. Os seis testes focados e a regressão completa passaram **128/128 em 2,2 min**. O adapter continua em memória; validação de magic bytes, storage e persistência reais estão adiados.

No refinamento do player M05, o chrome foi ancorado ao rodapé com gradiente transparente, progresso e nove ações por ícones em linha única. A barra some após quatro segundos em reprodução, reaparece com input e permanece aberta durante pausa ou seleção de faixas. O modo comum oculta diagnóstico, recuperação, cenários e linguagem interna; `?review=1` conserva as fixtures. Quatro testes focados passaram e a [captura 1080p](../../milestones/M05-local-playback/evidence/player-streaming-1080.png) foi inspecionada. A regressão completa passou **130/130**; vídeo/MPV e hardware físico continuam fora desta evidência.

Na correção seguinte, Filmes M02 recebeu o mesmo shell superior da Home; lateral, barra inferior e avisos internos de mock/provisoriedade foram removidos. Importação, filtros, favorito e ferramentas de detalhe passaram a ícones acessíveis, mantendo ações primárias textuais. Capturas 1080p/1440p/4K foram renovadas e inspecionadas. Um teste de regressão dedicado impede o retorno do shell antigo; naquele ponto, a suíte passou em 117/117.

No refinamento atual de M04, o detalhe comum de Filme/Série recebeu backdrop cinematográfico, sinopse, gêneros, elenco disponível, badges de metadata, avaliação/votos, disponibilidade, identificadores, memberships e ações agrupadas. Voltar foi posicionado imediatamente depois de Assistir/Continuar e fecha o diálogo restaurando o foco, inclusive no modo TV. A prévia de trailer usa apenas o backdrop local com play/pause e progresso determinístico; não há vídeo, áudio, provider ou rede. O caso novo verifica as duas variantes, alinhamento e ordem das quatro ações, fechamento do trailer e retorno pelo novo botão. Os testes focados passaram **10/10**, o modal coube em 1080p/1440p/4K e a regressão completa passou **131/131 em 2,2 min**. Capturas: [Filme](../../milestones/M04-home-search/evidence/rich-movie-detail-1920.png), [trailer](../../milestones/M04-home-search/evidence/rich-movie-trailer-1920.png) e [Série](../../milestones/M04-home-search/evidence/rich-series-detail-1920.png).

Na extensão seguinte, o rodapé do modal recebeu seis Recomendados em cards 16:9. O ranking local exclui o item atual e usa tipo, gêneros compartilhados, memberships e avaliação; não alega personalização real. O teste abre uma recomendação no mesmo diálogo, comprova retorno ao topo e mantém o foco de saída associado ao card externo original. Typecheck, lint, cinco testes focados e a regressão completa **131/131 em 2,2 min** passaram. A [captura 1080p](../../milestones/M04-home-search/evidence/rich-series-recommendations-1920.png) foi inspecionada.

No ajuste dos catálogos M02/M03, Filmes e Séries receberam busca local e ordenação por destaque, quantidade de votos ou título. A normalização ignora acentos e caixa, divide a consulta em termos e procura em título localizado, original e gêneros. `Em destaque` conserva a ordem editorial da fixture; `Mais votados` usa a quantidade de votos do snapshot IMDb e `A–Z` usa o título apresentado. O teste conjunto valida os dois catálogos e as capturas [Filmes](../../milestones/M02-movies/evidence/catalog-search-sort-1920.png) e [Séries](../../milestones/M03-series/evidence/catalog-search-sort-1920.png) foram inspecionadas. Typecheck, lint, build e regressão completa passaram **132/132 em 2,3 min**. Não há FTS, provider ou ranking real.

Na correção transversal do cabeçalho M02/M03/M04, a marca textual `Ushark` foi convertida de botão para `span` estático. Ela tem `pointer-events: none`, `user-select: none`, `tabIndex -1` implícito e não expõe papel de botão; `Início` permanece como destino explícito de navegação. O teste dedicado percorre Home, Filmes e Séries e verifica as quatro garantias. A regressão completa passou **133/133 em 2,3 min**, incluindo layouts e modo TV.

No ajuste posterior, o detalhe de Filme foi convertido de modal para uma página canônica compartilhada por M02 e M04. As duas entradas usam `ContentDetails` e `#/content/:contentId`; a página ocupa toda a superfície, usa Hero cinematográfico, mantém informação, ações, trailer e Recomendados, e não expõe overlay nem papel de diálogo. A aba Filmes injeta somente suas ações contextuais de gestão. Voltar restaura o card, a consulta e o scroll; ao sair do player, o detalhe e o foco em Assistir/Continuar também são preservados. Série mantém seu modal e focus trap. A validação direcionada passou **48/48** em browser, Electron e 1080p/1440p/4K; a regressão completa, executada sem traces, passou **136/136 em 2,2 min**. Capturas: [Home/busca](../../milestones/M04-home-search/evidence/rich-movie-detail-1920.png) e [catálogo de Filmes](../../milestones/M02-movies/evidence/details-1920.png).

Na correção de contenção dos cards, `movie-card-media` e `discovery-card-media` passaram a fornecer o contexto de posicionamento e recorte para Torrent Health e favorito. A largura do badge é limitada à arte e seu label pode truncar sem deslocar as barras. `tests/card-containment.spec.ts` passou **2/2** num servidor exclusivo, sem trace, cobrindo 480, 700, 761, 1100 e 1920 px; uma inspeção da [busca em 1920 px](../../milestones/M04-home-search/evidence/card-containment-1920.png) confirmou os overlays dentro dos pôsteres. Lint, typecheck e build passaram. Uma tentativa anterior de regressão encontrou `ENOSPC` ao gravar traces; após remover somente artefatos temporários regeneráveis, a regressão integral sem trace passou 136/136 e passou a cobrir também esta correção.

No ajuste posterior do M18, o modo TV passou a ser somente de consumo. O Electron encaminha `--tv` ao renderer; Home, Filmes, Séries, detalhes e Player ocultam bibliotecas, downloads, cadastro, torrent, gestão de fontes, edição, diagnóstico, preferências e cenários. Busca, favoritos, reprodução, transporte, áudio e legendas continuam acessíveis. Dois novos testes cobrem a entrada direta e o detalhe de Filmes, elevando a regressão atual para 119/119. A inspeção visual de `tv-home-1080.png`, `tv-movies-1080.png` e `tv-player-1080.png` confirmou a superfície limpa; controles de fixture da sessão exigem `?review=1`.

No alinhamento estrutural com A11, o Electron foi movido para `apps/desktop/src/main`, ganhou um preload mínimo em `apps/desktop/src/preload` e todo o React/CSS foi concentrado em `apps/desktop/src/renderer`. `packages/mocks` passou a separar diretamente `data`, `services` e `scenarios`; seus consumidores usam exports públicos `@ushark/*`, com dependências de workspace declaradas. Dois testes novos verificam esses boundaries e a ausência de travessia para diretórios internos dos packages. O build e os seis testes Electron empacotados passaram após a migração.

No ajuste do conteúdo padrão de M02, consultas pontuais aos datasets oficiais IMDb `title.basics` e `title.ratings` confirmaram IDs, títulos originais, ano, duração, gêneros, notas e votos de oito filmes. Atendendo à correção visual do usuário, as páginas públicas desses títulos também forneceram oito pôsteres e oito imagens horizontais, materializados como snapshots locais. O renderer inicia o catálogo em memória com esse conteúdo sem request de rede, segredo ou hotlink. Os testes verificam quantidade, IDs externos, busca pelo título original, caminhos/carregamento de todas as capas e backdrop offline no Electron. Evidência detalhada: [IMDB_SEED](../../milestones/M02-movies/evidence/IMDB_SEED.md).

Correções transversais auditadas:

- Aplicar limite de cache conserva caminho quando volume não foi alterado; retenção de entrada externa apresenta volume coerente.
- Restaurar backup não retrocede o contador de IDs de curadoria.
- Sessões/downloads/probes rejeitam limites fracionários.
- Áudio/legenda do onboarding chegam ao player; conteúdo assistido deixa a seção de continuação de curadorias.
- Seleção automática desligada exige escolha explícita da fonte do próximo, mesmo com autoplay habilitado.
- Gamepad sintético altera slider horizontalmente; teste aguarda a restauração de foco do modal antes de operar o slider.
- Captura M22 aguarda candidato pronto e M17 aguarda transição visual; evitam registrar estado transitório como resultado.

Testes adicionais usam o mesmo frontend e adapters. A ação de copiar código M15 é interceptada no boundary do clipboard do navegador de teste; não modifica clipboard do usuário nem envia/publica dados externos.

## Inspeção visual

Cada milestone M05–M22 registra inspeção e ajustes em seu evidence/VALIDATION.md; M01–M04 conservam evidências anteriores e recebem regressão. Capturas do navegador em 1080p/1440p/4K estão nas respectivas pastas evidence. A Home M04 foi recapturada e inspecionada após o ajuste de streaming; a auditoria final também inspecionou diretamente candidato M22 em 1080p, prévia M17 e player em 4K. O player 4K foi exibido reduzido pela ferramenta para inspeção geral; isso não valida leitura à distância. M17 foi recapturado após aguardar a transição de aba.

Capturas verificam composição, texto, estados e limites da superfície. Não constituem aprovação humana. TV/controle físicos, overscan e legibilidade a distância permanecem pendentes.

## Pendências preservadas

Build emite aviso de chunk JS acima de 500 kB (aproximadamente 589 kB minificado / 175 kB gzip). Build passa; não há prova de budgets cold/warm startup, 60 FPS, memória ou latência em hardware alvo. Esta fase não calibra o motor de Health/probes nem comprova buffering real.

Windows x64, Sunshine/Moonlight/LAN, captura sobre vídeo, codecs/GPU/áudio, controle físico, IO em volumes, SQLite/WAL, crash/restart reais, download/probing reais, parser/ZIP/SSRF, Ed25519/secure storage, backend/Registry, instalador/code signing/SBOM/provenance/CI e promoção Stable continuam adiados para S03–S08 mediante autorização. Nenhum requisito integral de durabilidade/segurança/performance foi aprovado com base em mocks.

No refinamento de ações dos detalhes, o componente compartilhado passou a renderizar Voltar somente como ícone à esquerda de Assistir/Continuar e Trailer imediatamente à direita; Baixar e ações contextuais seguem depois. O ícone mantém `aria-label` e tooltip. Um marcador semântico próprio identifica a ação principal, permitindo foco determinístico na abertura por M02/M04, no modal de Série, após recomendações e no retorno do player. A inspeção de [página completa 1080p](../../milestones/M04-home-search/evidence/rich-detail-1920.png) confirmou ordem, alinhamento e tamanho compacto do retorno. Quinze testes direcionados e a regressão completa **136/136 em 2,2 min** passaram; Windows/TV/gamepad físicos continuam pendentes.

Na correção atual dos cards de Filmes, `movie-card-media` e a nova `movie-card-copy` compõem uma única superfície com borda, fundo e recorte. Título, ano/fontes/qualidade, IMDb, votos, identificador e gêneros têm limites próprios; no intervalo até 1000 px, três colunas substituem as quatro que comprimiam a metadata. O teste de contenção passou a validar todos esses elementos, além de Health/favorito, em 480, 700, 761, 1100 e 1920 px. A inspeção da [captura em 761 px](../../milestones/M02-movies/evidence/card-internals-761.png) confirmou os componentes dentro dos cards. Dezenove testes focados, lint, typecheck, build e a regressão completa **137/137 em 2,2 min** passaram.

No ajuste global seguinte, `styles.css` passou a definir o scroll vertical com o accent Ushark `#87bbef`, trilho `#0a1118`, largura fina, borda arredondada e hover `#a0cfff`. A implementação combina as propriedades padronizadas com os pseudos WebKit aceitos pelo Chromium/Electron; regras específicas continuam ocultando apenas os trilhos horizontais editoriais. `scrollbar.behavior.spec.ts` verifica cor e largura computadas. O caso dedicado mais os layouts M01–M04 passaram **19/19**; lint e typecheck passaram. A primeira tentativa do build Turbo não conseguiu criar seu log por `ENOSPC`; foram removidos somente `.turbo/cache`, o `dist` anterior e o cache local `.pnpm-store`, todos regeneráveis. O build seguinte passou e a regressão isolada sem traces fechou em **138/138 em 2,2 min**.

No refinamento de hierarquia do detalhe, o bloco compartilhado de ações passou a ficar imediatamente abaixo do Hero e acima da sinopse, tanto na página canônica de Filme quanto no modal de Série. A ordem Voltar, Assistir/Continuar, Trailer, Baixar e ferramentas contextuais, o foco inicial na reprodução e todos os handlers foram preservados. O teste rico verifica a ordem semântica e a posição vertical nas duas variantes; os seis layouts de Discovery/Filmes repetem a geometria em 1080p, 1440p e 4K. A validação direcionada passou **8/8**; lint, typecheck, build e a regressão completa passaram **138/138 em 2,2 min**. A inspeção de [Filme](../../milestones/M04-home-search/evidence/rich-movie-detail-1920.png) e [Série](../../milestones/M04-home-search/evidence/rich-series-detail-1920.png) confirmou ações antes da sinopse.

No ajuste fino seguinte da página de Filme, `discovery-detail-page` passou a sobrescrever somente o espaçamento superior das ações de 28px para 8px. Isso aproxima todo o conteúdo subsequente da base do Hero sem sobreposição e sem alterar o modal de Série. Os seis testes de layout medem a distância entre o fim do Hero e o primeiro botão e exigem no máximo 16px em 1080p, 1440p e 4K; o teste rico preserva ordem, foco e trailer. Os **7/7 testes focados**, lint, typecheck, build e a regressão completa **138/138 em 2,2 min** passaram. A [captura de Interestelar](../../milestones/M04-home-search/evidence/rich-movie-detail-1920.png) foi regenerada e inspecionada.

Na recomposição seguinte da página de Filme, `ContentDetails` passou a renderizar título, badges e `SourceChoices` dentro do mesmo `discovery-detail-hero-copy`. Um grid próprio do Hero coloca esse bloco à esquerda e a sinopse, gêneros e elenco em um card translúcido à direita; os fatos continuam no fluxo abaixo. Ao abrir o trailer, todo o grid editorial some e os controles do trailer permanecem isolados. O modal de Série conserva sua composição anterior. Os **7/7 testes focados** verificam pertencimento DOM, título acima das ações, sinopse à direita e todos os elementos dentro do Hero em 1080p, 1440p e 4K. Lint, typecheck e build passaram. A primeira regressão completa terminou com **137/138** por timeout de 30s no teste Electron M02; o mesmo caso passou isolado em **3,1s**, e a repetição completa fechou em **138/138 em 2,2 min**, sem mudança de produto. Capturas inspecionadas: [detalhe](../../milestones/M04-home-search/evidence/rich-movie-detail-1920.png) e [trailer](../../milestones/M04-home-search/evidence/rich-movie-trailer-1920.png).

No ajuste atual de A09, `renderer/app/navigation.ts` identifica o primeiro botão habilitado e visível do scope ativo. Quando o foco navega para esse botão, a página volta a `top: 0`; em diálogos roláveis, o próprio scope volta ao início. Outros elementos preservam o `scrollIntoView` por proximidade e a preferência de movimento reduzido continua respeitada. `navigation.behavior.spec.ts` cobre a Home rolada, o movimento de Filmes para Início, o foco final e `window.scrollY = 0`; a suíte direcionada passou **3/3**. A primeira tentativa global com traces foi descartada após `ENOSPC` durante a gravação de artefatos; dois sinais funcionais foram repetidos isoladamente e passaram **2/2**. A repetição completa com traces desativados passou **139/139 em 2,4 min**. Build, typecheck, lint completo e formatação dos arquivos alterados passaram.

No refinamento de Recomendados, `renderer/catalog/DiscoveryRail.tsx` passou a concentrar arte, sinal da fonte, card e trilho usados tanto na Home quanto em `ContentDetails`. A seção final de Filme/Série reutiliza a mesma superfície 16:9 de `Filmes para descobrir` e `Continuar assistindo`, com tipo/ano/qualidade, Torrent Health e progresso opcional; o ranking e a navegação dentro do detalhe não mudaram. O teste rico verifica as classes compartilhadas, a ausência do título atual, a troca na mesma superfície e o foco inicial em reprodução. A inspeção de [rich-detail-1920.png](../../milestones/M04-home-search/evidence/rich-detail-1920.png) confirmou o trilho. Após a reorganização A11 concorrente terminar e o build Electron ser regenerado, a suíte focada passou **19/19** e a regressão integral limpa passou **141/141 em 2,2 min**. Lint completo, typecheck e build passaram; o hash local foi atualizado.

No alinhamento vertical posterior da página de Filme, o grid do Hero passou de alinhamento pela base para centralização vertical explícita das duas colunas; `hero-copy` e `hero-overview` usam o mesmo centro. `discovery-detail-content`, `discovery-detail-scenarios` e `discovery-recommendations` agora compartilham largura `min(1440px, 90vw)` e margem central. Uma verificação Playwright sem escrita de imagens mediu `centerDelta` 0 px em 1920/2560 e 0,01 px em 3840; as três guias laterais coincidiram em cada resolução, sem overflow. Em 480 px, a sinopse empilha abaixo do bloco principal e a página mantém 480 px sem overflow. Os layouts completos M02/M04 em 1920 passaram **2/2** e a [captura de Interestelar](../../milestones/M04-home-search/evidence/rich-movie-detail-1920.png) foi inspecionada. Lint, typecheck, build Vite direto e hashes passaram. A primeira suíte ampla do ajuste interrompeu a escrita de capturas com `ENOSPC`; isso não foi registrado como falha funcional nem como nova regressão integral. A última regressão integral anterior permanece **141/141**.

No ajuste de transparência do detalhe, o fundo de `discovery-detail-hero-overview` passou a `rgba(8, 12, 18, 0.58)` e preservou `backdrop-filter: blur(14px)`. O conteúdo não recebeu `opacity`, mantendo contraste. Os testes de layout M02/M04 validaram estilo computado e geometria em 1080p/1440p/4K; dois fluxos adicionais renovaram [a captura de Interestelar](../../milestones/M04-home-search/evidence/rich-movie-detail-1920.png) e o print M02, totalizando **8/8**. A tentativa paralela inicial foi interrompida por `ENOSPC` ao gravar traces/capturas 4K; removidos apenas artefatos temporários, a repetição sequencial sem traces passou. A regressão integral anterior continua **141/141**.

## Filme sem faixa duplicada e com recomendações após o Hero

`ContentDetails` não renderiza mais `discovery-detail-facts` na página canônica de Filme: título original, nota/votos, disponibilidade e IMDb deixaram de ser repetidos abaixo do Hero. O modal de Série conserva a faixa, seus episódios e demais fatos. Recomendados é agora o irmão DOM imediato do Hero no filme e os cenários de revisão aparecem somente depois do trilho. O padding assimétrico do grid eleva discretamente título/sinopse, preservando centro vertical idêntico nas larguras de TV. O teste rico M04 e os layouts M02/M04 em 1920 passaram **3/3**. A medição sem capturas confirmou zero fatos de Filme, gap estrutural de 0 px até Recomendados, ausência de overflow em 1920/2560/3840/480 e um bloco de fatos preservado no modal de Série. Lint, typecheck, build Vite e hashes passaram; o aviso conhecido do chunk acima de 500 kB permanece. A [captura 1920](../../milestones/M04-home-search/evidence/rich-detail-1920.png) foi regenerada e inspecionada. A regressão integral anterior continua **141/141**.

## Catálogos de Filmes e Séries por categorias

As superfícies padrão M02/M03 agora reutilizam o padrão editorial da Home: um trilho `Em destaque` e trilhos panorâmicos por categorias amplas, mantendo Health, metadata, imagens locais e boundaries mockados. Busca, Favoritos e ordenações explícitas continuam em grade vertical. O retorno do detalhe restaura o card exato mesmo quando o mesmo conteúdo aparece em mais de um trilho. Typecheck, lint e build passaram; a regressão browser isolada do servidor de desenvolvimento passou **142/142 em 2,1 min**, e os dois fluxos Electron M02/M03 passaram **2/2**. A execução contra o servidor Vite ativo foi descartada porque alterações concorrentes do M01 provocaram recargas durante os testes. [Relatório detalhado](CATALOG_CATEGORY_RAILS.md).

## Busca recolhida em Filmes e Séries

O campo permanente foi substituído por uma lupa no grupo direito de ações dos catálogos M02/M03. O input só existe após ativação, recebe foco imediatamente e é recolhido com limpeza da consulta por fechamento ou Voltar/Escape; o foco retorna à lupa antes de uma eventual saída da tela. A suíte direcionada passou **3/3**, a regressão browser contra build estático passou **144/144 em 2,2 min**, e os fluxos Electron M02/M03 passaram **2/2**. Typecheck, lint, build e inspeção visual dos estados recolhido/expandido passaram; o aviso conhecido do chunk acima de 500 kB permanece. [Relatório detalhado](COLLAPSED_CATALOG_SEARCH.md).
